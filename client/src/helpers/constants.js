

const snackbarPayload = {
    snackbarText: '',
    snackbarSeverity: '',
}

const LOGOUT = 'LOGOUT'
const LOGIN = 'LOGIN'
const OPENSNACKBAR = 'OPENSNACKBAR'
const CLOSESNACKBAR = 'CLOSESNACKBAR'

export  {
    snackbarPayload,
    LOGOUT,
    LOGIN,
    OPENSNACKBAR,
    CLOSESNACKBAR
}
